import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import { CssImportBlock, WebScriptBlock } from './InstallSnippets';
import PreferencesPanel from '@site/src/components/PreferencesPanel';

# Utvikler

Kom i gang med Indeks i prosjektet ditt. Velg teknologi under, så tilpasser guiden seg.

## Velg teknologi

<PreferencesPanel showHeading={false} />

## Installasjon

### 1. Fonter

Legg til SB1-fontene i `<head>` i HTML-dokumentet ditt. Monorepo-generatoren gjør dette automatisk.

```html
<link rel="stylesheet" href="https://cdn.sparebank1.no/fonts/v1.css" />
```

### 2. Styling

Importer CSS-en fra CDN inn i hoved-CSS-filen din, og legg `ix-body`-klassen på `<body>`. CDN-URL-en er den samme på tvers av SB1-applikasjoner, så nettleseren kan gjenbruke samme cachede CSS.

<CssImportBlock />

```html
<body class="ix-body">
```

Du får komponentstyling, [utility-klasser](/docs/utility-klasser/oversikt) og [designtokens](/docs/grunnleggende/tokens/introduksjon) i samme pakke.

<Tabs groupId="variant">
<TabItem value="html" label="HTML / web components">

### 3. Web components

Last inn web components-scriptet fra CDN i `index.html`.

<WebScriptBlock />

Komponentene er nå tilgjengelige som HTML-elementer:

```html
<ix-field>
    <label>E-postadresse</label>
    <div class="ix-text-field">
        <input type="email" name="email" />
    </div>
    <span data-field="error"></span>
</ix-field>
```

Vil du ha TypeScript-typer for `<ix-field>` og de andre elementene? Installer `@sb1/indeks-web` som `devDependency` — pakken registrerer typene globalt via `HTMLElementTagNameMap` og JSX.

```bash
npm install --save-dev @sb1/indeks-web
```

Runtime-koden lastes fra CDN, så ingenting av pakken havner i den bygde applikasjonen.

</TabItem>
<TabItem value="react" label="React">

### 3. Web components

Last inn web components-scriptet fra CDN i `index.html`. `@sb1/indeks-react` er et tynt lag oppå web components og rendrer `<ix-*>`-elementer internt, så scriptet må være lastet for at React-komponentene skal virke.

<WebScriptBlock />

</TabItem>
</Tabs>

<Tabs groupId="variant">
<TabItem value="react" label="React">

### 4. React-komponenter

Installer React-pakken. `@sb1/indeks-web` tas som `devDependency` — den brukes kun til TypeScript-typer, runtime lastes fra CDN (steg 3).

```bash
npm install @sb1/indeks-react
npm install --save-dev @sb1/indeks-web
```

Importer komponentene der du skal bruke dem:

```javascript
import { Button, Text } from '@sb1/indeks-react';
```

```jsx
function App() {
    return (
        <div>
            <Text>Innhold</Text>
            <Button>Klikk meg</Button>
        </div>
    );
}
```

</TabItem>
<TabItem value="html" label="HTML / web components">

</TabItem>
</Tabs>

## Installere via npm i stedet for CDN

`@sb1/indeks-css` og `@sb1/indeks-web` finnes også på npm, men vi anbefaler CDN. Med CDN deler alle SB1-applikasjoner samme URL, så nettleseren kan gjenbruke cachen på tvers av apper — brukerne slipper å laste ned CSS og web components på nytt når de bytter mellom SB1-tjenester.

Hvis du likevel vil bundle det selv (f.eks. intern app uten CDN-tilgang):

```bash
npm install @sb1/indeks-css @sb1/indeks-web
```

## Hva nå?

- Lær om [designtokens](/docs/grunnleggende/tokens/introduksjon) for å lage egne komponenter
- Bruk [utility-klasser](/docs/utility-klasser/oversikt) for rask styling
- Se [mønstre og maler](/docs/monstre-og-maler/layout) for vanlige bruksområder
- Migrering: [spacing fra FFE](/docs/grunnleggende/tokens/spacing#migrere-fra-ffe), [spacing fra Tailwind](/docs/grunnleggende/tokens/spacing#migrere-fra-tailwind), [farger fra FFE](/docs/retningslinjer/farger/#migrere-fra-ffe)

## Teknisk struktur

De fleste trenger ikke å forholde seg til dette, men du kan lese videre hvis du er spesielt interessert.

Indeks er bygget opp av 5 pakker:

<ul className="ix-list-disc ix-pl-md">
    <li className="ix-py-2xs">indeks-tokens</li>
    <li className="ix-py-2xs">indeks-utils</li>
    <li className="ix-py-2xs">indeks-css</li>
    <li className="ix-py-2xs">indeks-web</li>
    <li className="ix-py-2xs">indeks-react</li>
</ul>

Vi har også en mappe med dokumentasjonen og en eksempel-app vi bruker til å teste.

<img src="/img/kom-i-gang/teknisk-struktur.png" />
